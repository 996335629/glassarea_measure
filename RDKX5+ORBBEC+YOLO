RDKX5+ORBBEC+YOLO
✅ 阶段0：环境准备（只跑一次）
bash
# 1. 安装 ROS2 驱动、编解码、WebSocket 及 DNN 节点
sudo apt update
sudo apt install -y ros-humble-orbbec-camera ros-humble-hobot-codec ros-humble-websocket

# 2. 将 TROS 环境写入 bashrc（避免每次手动 source）
echo "source /opt/tros/humble/setup.bash" >> ~/.bashrc
source ~/.bashrc

# 3. 验证 ROS2 环境
which ros2   # 应输出 /opt/ros/humble/bin/ros2
✅ 阶段1：释放 8000 端口 & 清理旧进程
bash
pkill -9 -f "ros2 launch" 2>/dev/null
pkill -9 -f "websocket --ros-args" 2>/dev/null
pkill -9 -f "hobot_codec_republish" 2>/dev/null
pkill nginx 2>/dev/null
sleep 2

# 确认端口已释放
ss -tlnp | grep :8000 || echo "✅ 8000端口空闲"
✅ 阶段2：启动 Orbbec 摄像头节点（后台）
bash
nohup ros2 launch orbbec_camera gemini_330_series.launch.py > /tmp/orbbec.log 2>&1 &
echo "Orbbec PID: $!"
sleep 12   # 等待节点初始化

# 验证：看节点列表 + 彩色图帧率
ros2 node list | grep camera   # 应看到 /camera/camera
ros2 topic hz /camera/color/image_raw &
sleep 4
pkill -f "ros2 topic hz"
✅ 通过条件：/camera/color/image_raw 频率约 28~30 Hz。

✅ 阶段3：上传自定义 launch 文件（在本地 PC 上执行）
将工作区中的 yolo_orbbec.launch.py 上传至 X5 的 /root/ 目录。

方法A（RDK Studio 文件面板）
在 RDK Studio 侧栏 Files 中直接拖拽到 /root/

方法B（Windows PowerShell / Linux 终端）

powershell
scp C:\Users\Lenovo\.rdkstudio\dmoss-workspaces\zhang_xiaoyu\yolo_orbbec.launch.py root@192.168.0.111:/root/
（请将 IP 改为你 X5 的实际地址）

验证（回到 X5 终端）：

bash
ls -l /root/yolo_orbbec.launch.py   # 应看到文件存在
✅ 阶段4：一键启动 YOLO 推理 + JPEG 编码 + WebSocket 推流
bash
cd /root
nohup ros2 launch yolo_orbbec.launch.py > /tmp/yolo.log 2>&1 &
echo "YOLO链路 PID: $!"
sleep 8   # 等待节点启动

# 4.1 检查三个核心节点是否运行
ros2 node list | grep -E "(dnn_yolo_orbbec|jpeg_codec|websocket)"

# 4.2 确认检测话题有数据
timeout 3 ros2 topic echo /hobot_dnn_detection --once | grep results

# 4.3 确认带框图像话题频率（应 ≥25 Hz）
ros2 topic hz /image_jpeg &
sleep 4
pkill -f "ros2 topic hz"

# 4.4 确认 8000 端口已被 nginx 监听
ss -tlnp | grep :8000

# 4.5 查看 YOLO 日志，确认无 fatal 错误
tail -30 /tmp/yolo.log
✅ 通过条件：三个节点全在、/image_jpeg 频率 ≥25 Hz、8000 端口监听、日志无 [FATAL]。

✅ 阶段5：在笔记本电脑浏览器中查看实时画面
打开浏览器（推荐 Chrome 无痕模式），访问：

text
http://192.168.0.111:8000
应看到实时视频流，并且行人、杯子、笔记本等物体上叠加了 YOLO 检测框。

如果打不开，按顺序检查：

PC 和 X5 是否在同一网段？ping 192.168.0.111
8000 端口是否放行？ss -tlnp | grep :8000
查看日志：tail -f /tmp/yolo.log
✅ 阶段6：停止所有服务（用完关机或重启前执行）
bash
pkill -f "yolo_orbbec"        # 停止 YOLO / 编码 / WebSocket
pkill -f "orbbec_camera"      # 停止摄像头节点
pkill nginx                   # 停止 web 服务
🔁 完整一键执行版（可选）
如果你已经完成了阶段0，并且 yolo_orbbec.launch.py 已经放在 /root/ 下，可以直接运行下面这个整合脚本（在 X5 终端执行）：

bash
#!/bin/bash
# 完整自动执行目录（从阶段1到阶段5）

set -e  # 遇错停止

echo "==> 清理旧进程"
pkill -9 -f "ros2 launch" 2>/dev/null || true
pkill -9 -f "websocket" 2>/dev/null || true
pkill -9 -f "hobot_codec" 2>/dev/null || true
pkill nginx 2>/dev/null || true
sleep 2
ss -tlnp | grep :8000 || echo "8000端口空闲"

echo "==> 启动 Orbbec 摄像头"
nohup ros2 launch orbbec_camera gemini_330_series.launch.py > /tmp/orbbec.log 2>&1 &
sleep 12
ros2 node list | grep camera

echo "==> 启动 YOLO 推理链路"
cd /root
nohup ros2 launch yolo_orbbec.launch.py > /tmp/yolo.log 2>&1 &
sleep 8
ros2 node list | grep -E "(dnn_yolo|jpeg_codec|websocket)"

echo "==> 验证图像话题频率"
timeout 4 ros2 topic hz /image_jpeg || true

echo "==> 验证 8000 端口"
ss -tlnp | grep :8000

echo "✅ 全部启动完成，请打开浏览器访问 http://192.168.0.111:8000"
将以上脚本保存为 start_vision.sh，然后：

bash
chmod +x start_vision.sh
./start_vision.sh
